name = "John"
print(f"{name[0]}")

names = ["Mary", "John", "Lucas", "Tom"] #this is a list and is mutable, as in, the values inside the list can change
print(f"{names[1]}")

#tuples
#the grouping of 2 similar values to 1 variable
#e.g
coordinate = (10.0, 20.0)#immutable, values can't be changed in tuples